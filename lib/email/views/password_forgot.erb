<%
   reset_url = "#{root_url}/password/change/#{url_encode email_token}"
%>
<% if defined?(name) %>
  <h1><%= name %>,</h1>
<% else %>
  <h1>Hello,</h1>
<% end %>
<p class="lead">
  Forgot your password?
</p>
<p>
  We received a request to reset the password for your account. To reset your password, click on the button below within three days:
</p>
<table class="medium-button">
  <tbody>
  <tr>
    <td>
      <a href="<%== reset_url %>" style="display: block;">Reset your password now</a>
    </td>
  </tr>
  </tbody>
</table>
<br/>
<p>
  Or copy and paste the URL into your browser:
  <blockquote>
    <a href="<%== reset_url %>"><%== reset_url %></a>
  </blockquote>
</p>
<p>
  If you're getting a lot of password reset emails you didn't request, please contact us:
  <blockquote>
    <a href="mailto:<%== $settings.customer_service_email %>?subject=<%== url_encode 'RE: Password reset requests' %>"><%== $settings.customer_service_email %></a>
  </blockquote>
</p>